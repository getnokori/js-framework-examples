{
  "dependencies": {
    "@nokori/js-sdk": "^1.0.64"
  },
  "devDependencies": {
    "nuxt": "^3.1.2"
  },
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "postinstall": "nuxt prepare",
    "preview": "nuxt preview"
  }
}
